node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json
rules.json
CLAUDE.local.md

# Quarantined pilot/no-peek results -- must never be committed while sealed
quarantine/
*_SEALED.json

# Proprietary strategy/research -- this repo's origin is public (tradesdontlie/tradingview-mcp)
research/
backtest/
velox/

# Chuck's personal ops scripts and proprietary trading protocol files -- same public-repo boundary
# as CLAUDE.local.md above. These were sitting untracked (not yet gitignored) as of 2026-07-22;
# already correctly backed up via the private chuck-alpha repo instead (see feedback_repo_separation).
.gemini/
session_heartbeat.txt
skills/preflight/
speak.ps1
speak_el.ps1
scripts/heartbeat_watchdog_check.ps1
scripts/launch_chrome_tv.bat
scripts/trading_window_watchdog.ps1
scripts/replay_20260722.py
